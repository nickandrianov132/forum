     type Post {
         id: ID!
         title: String!
         content: String!
         user: User
         category: Category!
           # Можно также добавить поля для счета лайков и дислайков
        #  likes: [Like!]!
        #  dislikes: [Dislike!]!
        # Теперь это обязательные поля (спасибо DataLoader)
         likesCount: Int!
         dislikesCount: Int!
        # Полезно знать: лайкнул ли пост текущий юзер?
         isLiked: Boolean!
         isDisliked: Boolean!    
         isOwner: Boolean!
         createdAt: String!
     }
    type Query {
        posts: [Post!]!
        post(id: ID!): Post 
        postsByCategory(categoryId: ID!): [Post!]!
    }
    type Mutation {
        createPost(categoryId: ID!, title: String!, content: String!, userId: ID!): Post
        updatePost(id: ID!, categoryId: ID, title: String, content: String): Post
        deletePost(id: ID!): Boolean

        # Лайки и Дизлайки (возвращают Post для мгновенного обновления кэша)
        addLike(postId: ID!): Post!
        addDislike(postId: ID!): Post!
    }