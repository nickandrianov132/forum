type Dislike {
    id: ID!
    user: User!
    post: Post!
    userId: ID!
    postId: ID!
    # Если вы добавили timestamps: true в Mongoose:
    createdAt: String
}

type Mutation {
    addDislike(postId: ID!): Post!
}